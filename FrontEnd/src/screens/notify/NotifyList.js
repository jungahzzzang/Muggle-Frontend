import React from "react";
import {Text} from "react-native";
import ScreenContainer from "../../components/ScreenContainer";

const NotifyList = () => {
    return(
        <ScreenContainer>
            <Text style={{fontSize: 24}}>Notify List</Text>
        </ScreenContainer>
    );
};

export default NotifyList;